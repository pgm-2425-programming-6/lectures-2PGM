import EmptyView from "@design/View/EmptyView";

const Projects = () => {
  return (
    <EmptyView
      icon="folder"
      title="Geen projecten"
      description="Voeg je eerste project toe om te starten"
      onPress={() => {}}
    />
  );
};

export default Projects;
